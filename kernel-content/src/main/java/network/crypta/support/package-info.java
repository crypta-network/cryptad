/**
 * Content-slice support helpers extracted into {@code :kernel-content}.
 *
 * <p>The package currently anchors MIME helper ownership for {@link
 * network.crypta.support.MediaType} and the manifest sizing helper {@link
 * network.crypta.support.ContainerSizeEstimator} so content and filter code can share them without
 * depending on runtime-node.
 */
package network.crypta.support;
