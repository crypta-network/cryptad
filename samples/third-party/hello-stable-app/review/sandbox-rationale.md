# Sandbox rationale

This sample uses a static UI and a placeholder launcher. It declares `sandbox.mode=none` only
because it has no background runtime behavior in the fixture. A production third-party app must
document the sandbox provider it needs and why.
