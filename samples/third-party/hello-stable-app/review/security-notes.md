# Security notes

The sample does not log browser session tokens, app tokens, private keys, private insert URIs,
fetched payload bodies, app-owned record values, or authorization headers. Reports should include
only app id, version, capability names, review status, digests, and redacted fixture URIs.
