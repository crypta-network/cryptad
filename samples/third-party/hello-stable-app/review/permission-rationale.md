# Permission rationale

`platform.contract.read` is required so the sample can display the local Platform API contract
version and stable baseline metadata. The app does not request queue, content, app-data, vault,
service, internal, or operator-only capabilities.
