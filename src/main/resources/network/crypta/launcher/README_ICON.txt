Place the PNG for the launcher Dock/window icon here as
`network/crypta/launcher/crypta-launcher-icon.png`.

Recommended sizes: 256x256 or 512x512 with transparency.

At runtime, the launcher will first try to load this resource. When running from
source without resources, it falls back to `docs/images/crypta_logo.png`.
